<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <div class="center margin-top width">
        <h1 class="text-center">Edit post</h1>
        <form action="/edit/<%= post._id%>" method="POST">
            <label>Title</label><br />
            <input type="text" name="title" maxlength="40" value="<%= post.title%>"><br /><br />
            <label>Body</label><br />
            <textarea type="text" name="body" class="textarea" value="<%= post.body%>"><%= post.body%></textarea><br /><br />
            <button type="submit" class="right">SAVE UPDATE</button>
        </form>
    </div>
</body>
</html>
